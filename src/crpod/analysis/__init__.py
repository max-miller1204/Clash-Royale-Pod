"""Post-analysis stages that consume an `AnalysisResult`.

Currently houses only blunder detection — additional verdict-style passes
(e.g. tempo summarizers, deck-archetype tagging) would land here.
"""
